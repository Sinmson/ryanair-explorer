meta {
  name: active
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/views/locate/5/airports/{{localeShort}}/active
  body: none
  auth: none
}

tests {
  test("Status is 200", function () {
    expect(res.status).to.equal(200);
  });
}
